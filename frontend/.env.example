# Backend API base. In local Vite development /api is proxied to localhost:5000.
VITE_API_BASE=/api

# Only set this in a controlled demo environment when DEMO_LOGIN_ENABLED=true on the backend.
VITE_DEMO_LOGIN_KEY=replace-with-the-demo-key
